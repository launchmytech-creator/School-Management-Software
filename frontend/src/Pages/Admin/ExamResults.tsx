import React from 'react';
import ExamResults from '../../components/common/ExamResults';

const ExamResultsPage: React.FC = () => {
  return <ExamResults layout="admin" />;
};

export default ExamResultsPage;
