import React from 'react';
import ExamResults from '../../components/common/ExamResults';

const AccountantExamResults: React.FC = () => {
  return <ExamResults layout="accountant" />;
};

export default AccountantExamResults;
