import React from 'react';
import StudentForm from '../../components/common/StudentForm';

const AccountantAddStudent: React.FC = () => {
  return <StudentForm layout="accountant" mode="create" />;
};

export default AccountantAddStudent;
