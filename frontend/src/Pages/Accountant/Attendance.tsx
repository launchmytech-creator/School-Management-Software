import React from 'react';
import SharedStudentAttendance from '../../components/common/StudentAttendance';

const AccountantAttendance: React.FC = () => {
  return <SharedStudentAttendance layout="accountant" />;
};

export default AccountantAttendance;
