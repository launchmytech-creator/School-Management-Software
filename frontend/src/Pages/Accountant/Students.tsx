import React from 'react';
import StudentsList from '../../components/common/StudentsList';

const AccountantStudents: React.FC = () => {
  return <StudentsList layout="accountant" />;
};

export default AccountantStudents;
