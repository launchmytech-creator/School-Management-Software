import React from 'react';
import ExamsList from '../../components/common/ExamsList';

const AccountantExams: React.FC = () => {
  return <ExamsList layout="accountant" />;
};

export default AccountantExams;
